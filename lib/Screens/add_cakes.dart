import 'package:flutter/material.dart';

class AddCakes extends StatelessWidget {
  const AddCakes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
